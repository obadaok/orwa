# مهام نظام مظاهر المرتّل (Murattal Theme System)

## الهدف
تحويل مظهر المرتّل من «تغيير ألوان جزئي» إلى نظام متكامل قائم على لوحة ألوان مركزية تطبَّق على كامل واجهة المرتّل، مع تجربة اختيار بصرية حقيقية.

## المهام

### 1. التحقيق والاستكشاف
- [x] قراءة `MurattalTheme.kt` (النظام الحالي: 8 ألوان × 5 مظاهر)
- [x] قراءة `SurahDetailActivity.applyMurattalTheme()` وتحديد الفجوات
- [x] فحص `view_focused_recitation.xml` و `view_audio_player_bar.xml` و `panel_play_window.xml`
- [x] فحص إعداد «مظهر المرتّل» في `fragment_settings.xml` و `SettingsFragment.kt`
- [x] حصر العناصر غير المشمولة: شريط المشغّل، الآفاتار، السيكب، نافذة التلاوة، نافذة السرعة، لحدود البطاقات

### 2. بناء نظام الألوان المركزي
- [x] توسيع `MurattalTheme` لتشمل لوحة كاملة (~18 لونًا) لكل مظهر
- [x] إنشاء `MurattalPalette` (قيم `Int` محلولة) + `resolve(context, theme)`
- [x] إضافة ألوان الـ 5 مظاهر الجديدة في `colors.xml`
- [x] أدوات برمجية لبناء drawables (سيكب، ثامب، بطاقات) من اللوحة

### 3. تطبيق المظهر على كامل المرتّل
- [x] إعادة كتابة `applyMurattalTheme` لتطبيق اللوحة على: الخلفية، الهيدر، بطاقة المعلومات، الآية، زخرفة
- [x] تطبيق اللوحة على شريط المشغّل (خلفية، آفاتار، نصوص، أزرار، سيكب)
- [x] تطبيق اللوحة على نافذة التلاوة (`panel_play_window`)
- [x] تطبيق اللوحة على نافذة السرعة في وضع المرتّل
- [x] إظهار/إخفاء الأزرار والحالات النشطة حسب اللوحة

### 4. صفحة اختيار المظهر البصرية
- [x] إنشاء `MurattalThemePickerActivity` + تخطيط الشبكة
- [x] إنشاء مكوّن معاينة مصغرة `MurattalThemePreview` يرسم واجهة المرتّل بلوحة المظهر
- [x] بطاقة لكل مظهر: اسم عربي + معاينة + علامة المحدد
- [x] الحفظ الفوري + العودة بالنتيجة لتطبيق فوري

### 5. إعادة تصميم إعداد «مظهر المرتّل»
- [x] تحويل الصف إلى بطاقة بهوية عروة مع معاينة مصغّرة لونية للمظهر الحالي
- [x] التنقل إلى صفحة الاختيار عند الضغط
- [x] تحديث العرض عند العودة

### 6. منع تسرب المظهر الافتراضي
- [x] التخلص من أي ألوان ثابتة متبقية في عناصر المرتّل
- [x] توحيد الطريقة: كل عناصر المرتّل تعتمد على `MurattalPalette`

### 7. قابلية التوسع + الحفظ
- [x] إضافة مظهر جديد = إضافة ألوان + تعريف فقط (بدون تعديل الواجهة)
- [x] التأكد من الحفظ الفوري والبقاء بعد الإغلاق/إعادة التشغيل

### 8. التسجيل والتثبيت
- [x] تسجيل `MurattalThemePickerActivity` في AndroidManifest
- [x] بناء المشروع وحل أي أخطاء

### 9. الاختبار الفعلي
- [ ] اختيار مظهر أول وفحص كامل المرتّل
- [ ] اختيار مظهر مختلف جذريًا وفحصه
- [ ] اختبار المشغّل، نافذة التلاوة، الإظهار/الإخفاء، التنقل، إعادة التشغيل

---

# مهام صفحة الأذكار (Dhikr Page)

## الهدف
تحسين تجربة صفحة الأذكار والصفحات المرتبطة بها: اتجاه السهم الصحيح لـ RTL، تباين الأيقونات، التحديث الفوري لحالة الاكتمال، حفظ تقدم كل ذكر بشكل مستقل بمفتاح هويته (id)، وإعادة ضبط مقصودة فقط.

## المهام

### 1. سهم الرجوع باتجاه RTL
- [x] إنشاء `ic_back_arrow_rtl.png` (يشير يمينًا) في كل مجلدات الكثافات
- [x] استخدامه في `activity_dhikr_details.xml` (btnBack)
- [ ] مراجعة بقية صفحات الأذكار/المرتبطة: عدم تغيير أسهم التنقل التزيينية (مثل `item_surah_card`) وأسهم التالي/السابق

### 2. تباين أيقونات البطاقة
- [x] تحويل tint أيقونات (مفضلة/مشاركة/نسخ) من `urwah_thread_light` إلى `urwah_thread_brown`
- [ ] التحقق البصري في الوضعين الفاتح والداكن على الجهاز

### 3. تحديث فوري لحالة الاكتمال
- [x] تطبيق البهتة/التعطيل (alpha 0.55 + isEnabled=false) فور بلوغ العدد في معالج النقر بدلًا من انتظار إعادة الربط
- [ ] التحقق بصريًا أن البطاقة تُبهت فورًا دون Scroll

### 4. حفظ التقدم المستقل لكل ذكر
- [x] حفظ العداد الجزئي لكل ذِكر بمفتاح هويته (id) في نفس SharedPreferences (`counts_<category>`)
- [x] استعادة العداد الجزئي في `setSavedProgress`
- [x] الاحتفاظ بحالة الاكتمال (لا تُعاد صفرًا عند فتح/إغلاق)
- [ ] إزالة أي أثر للعداد المحذوف مع ذِكر محذوف
- [ ] الاختبار: 66/100 → خروج/عودة → إكمال 100 → إعادة تشغيل

### 5. إعادة الضبط مقصودة من المستخدم فقط
- [x] `resetCategory` يمسح مفتاح العداد أيضًا (`counts_<category>`)
- [ ] مراجعة `checkAndResetDaily`: لا يصفّر إلا بتاريخ جديد مقصود
- [ ] لا تصفير عند: مغادرة الصفحة، إعادة فتحها، إعادة إنشاء الـ Activity، تبديل الوضع الليلي/النهاري

### 6. مراجعة دورة الحياة
- [ ] مراجعة `DhikrDetailsActivity` / `DhikrListAdapter`: لا فقدان للعداد عند التبديل بين النشاطات أو إعادة الإنشاء

### 7. الأداء
- [x] تحديث البطاقة المتأثرة فقط (تغيير محلي بلا `notifyDataSetChanged` كامل ولا فقدان موضع Scroll)
- [ ] التحقق من ثبات موضع التمرير أثناء الضغط المتكرر

### 8. الاختبار النهائي
- [ ] سيناريو: 66/100، خروج وعودة، إكمال 100، إعادة تشغيل، التحقق من حفظ الحالة والعدد
- [ ] فحص الأيقونات والسهم بصريًا في الوضعين
- [ ] تحديث TASKS.md بحالات المهام